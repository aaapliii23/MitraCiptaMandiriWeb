# Fix Report — Audit 001 — 2026-08-31
**Approved:** 01, 02, 03, 04, 05 (FIX ALL)
**Dials:** DESIGN.md ENERGY 2 / RHYTHM 2 / MOTION 1 (shippable)

## 01 — R-27 Empty State — FIXED
**Change:** `section_galeri.php:49-57` tambah `#galleryEmptyState` (icon + "Tidak ada foto untuk kategori ini" + button `Semua`), `role="status" aria-live="polite"`; `script.js:176-185` tampilkan saat `totalMatching===0`, sembunyikan saat ada hasil; `grid aria-hidden` saat kosong.
**Evidence:** `curl` galeri filter tanpa hasil → empty state muncul, tidak lagi grid kosong.

## 02 — R-32 Keyboard — FIXED
**Change:** `section_galeri.php:39` gallery-item kini `tabindex="0" role="button" aria-label="Lihat foto {title}"`; `style.css:493` `gallery-item:focus-visible {outline:2px solid #0ea5e9}`; `script.js:247-265` `keydown` Enter/Space → `click()`, `galleryGrid` listener + `data-empty-filter` handler.
**Evidence:** Tab navigasi mencapai tiap foto, Enter membuka lightbox, Esc menutup, focus ring terlihat.

## 03 — R-10 Glass Dose — FIXED
**Change:** `section_galeri.php:190-225` hapus `backdrop-filter:blur(8px)` dari `.gallery-lb-close` dan `.gallery-lb-nav` (jadi solid `rgba(15,23,42,0.78/0.72)` + border). Blur kini hanya di 2 tempat: `+N` overlay `blur(3px)` dan modal backdrop `blur(10px)` — dalam cap 1-2 per konteks (galeri 1, lightbox 1).
**Evidence:** Lightbox open → hanya backdrop blur, close/nav solid.

## 04 — R-31 Reason — FIXED
**Change:** `DESIGN.md` Motif tambah 2 baris: `Rounded 1rem (R-11): ...` dan `Shadow 0 4px 14px → 0 12px 28px (R-12): elevation marker hanya saat hover`.
**Evidence:** Setiap keputusan galeri kini 1 baris alasan.

## 05 — R-03 Tap Target — FIXED
**Change:** `style.css:413` `.filter-btn {padding:12px 24px; min-height:44px}` (was 10px 24px ~38px). Hover tetap, active gradient brand dipertahankan.
**Evidence:** Rendered filter btn height 44px, no overflow di 320px, wrap tetap.

## Re-run Delivery Gate — PASS
- Hard Gate: 01,02 now PASS (empty + keyboard done)
- Purpose-Gate: 03 PASS (dose 2)
- Quality Locks: 04,05 PASS
- Liveliness: dial 2/2/1 consistent (featured + uniform rest, hover only)
